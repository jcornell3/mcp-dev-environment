# ============================================================================
# MCP Development Environment Configuration
# ============================================================================
# Copy this file to .env and customize values for your environment

# ============================================================================
# HTTP Server Configuration
# ============================================================================

# Domain for HTTP/HTTPS configuration
DOMAIN=localhost

# HTTP port (typically 80, use 8080 to avoid sudo)
HTTP_PORT=80

# HTTPS port (typically 443, use 8443 to avoid sudo)
HTTPS_PORT=8443

# ============================================================================
# MCP API Keys & Authentication
# ============================================================================

# Default API key shared by all MCP servers
# Used for authentication between bridge and backend servers
MCP_API_KEY=default-api-key

# Bridge API token (for HTTP/SSE layer authentication)
# Used by Claude Desktop when connecting to bridge endpoints
BRIDGE_API_TOKEN=bridge-default-secret

# GitHub Personal Access Token (for github-remote server)
# Scope required: repo, gist, read:user
# Create at: https://github.com/settings/tokens
GITHUB_PERSONAL_ACCESS_TOKEN=

# ============================================================================
# Bridge Port Configuration
# ============================================================================
# Each MCP server is exposed through a dedicated HTTP/SSE bridge on a unique port
# These ports are exposed to the host and accessed by Claude Desktop

# Bridge port for math server (HTTP/SSE)
BRIDGE_MATH_PORT=3001

# Bridge port for santa-clara server (HTTP/SSE)
BRIDGE_SANTA_CLARA_PORT=3002

# Bridge port for youtube-transcript server (HTTP/SSE)
BRIDGE_YOUTUBE_TRANSCRIPT_PORT=3003

# Bridge port for youtube-to-mp3 server (HTTP/SSE)
BRIDGE_YOUTUBE_TO_MP3_PORT=3004

# Bridge port for github-remote server (HTTP/SSE)
BRIDGE_GITHUB_REMOTE_PORT=3005

# ============================================================================
# Service-Specific Configuration
# ============================================================================

# YouTube-to-MP3 Downloads Directory
# Maps from host to container
DOWNLOADS_DIR=/mnt/c/Users/jcorn/Downloads

# ============================================================================
# Environment
# ============================================================================
MCP_ENV=development
